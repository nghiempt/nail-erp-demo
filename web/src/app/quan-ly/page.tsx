import Artboard from "@/components/Artboard";
import html from "@/generated/QuanLy";

export const metadata = { title: "Điều hành ca — Quản lý" };

export default function Page() {
  return <Artboard html={html} width={1440} />;
}
