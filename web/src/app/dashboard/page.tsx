import Artboard from "@/components/Artboard";
import html from "@/generated/Main";

export const metadata = { title: "Tổng quan — Chủ tiệm" };

export default function Page() {
  return <Artboard html={html} width={1440} />;
}
